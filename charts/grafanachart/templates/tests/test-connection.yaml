apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "grafanachart.fullname" . }}-test-connection"
  labels:
    {{- include "grafanachart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "grafanachart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
