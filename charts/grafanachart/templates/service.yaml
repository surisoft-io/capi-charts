apiVersion: v1
kind: Service
metadata:
  name: {{ include "grafanachart.fullname" . }}
  labels:
    {{- include "grafanachart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grafanachart.selectorLabels" . | nindent 4 }}
