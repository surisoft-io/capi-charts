apiVersion: v1
kind: Service
metadata:
  name: {{ include "capichart.fullname" . }}
  labels:
    {{- include "capichart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "capichart.selectorLabels" . | nindent 4 }}
